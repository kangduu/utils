/**
 * @name date range 
 * @param {String} time
 * @returns {Array}  
 * @description 传递一个时间字符串参数，必须符合 'yyyy-MM-dd hh:mm:ss' 的格式，可以是其子集，如 'yyyy-MM-dd'。然后调用对应的方法返回对应的时间清单数组
 */
var DateRange = (function () {

    function dateRange(time) {

        this.getThisWeek = function () {

        }


    }
    dateRange.prototype = {

    }
    return dateRange
})()

